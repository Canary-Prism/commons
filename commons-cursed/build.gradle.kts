plugins {
    `java-library`
}


description = "A collection of cursed utilities for the commons library."

version = "1.0.4"

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}
